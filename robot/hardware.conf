# hardware.conf — Configuration des drivers materiels
# 1 = driver ARM reel, 0 = driver simu (stub)
# Fichier absent = tout active (mode match)

# Actions
LedDriver=1
LcdShieldDriver=1
ButtonDriver=1
SwitchDriver=1
SensorsDriver=1
ServoDriver=1
ColorDriver=1

# Asservissement
AsservDriver=1
